// 非 Web 平台实现（Windows、Android、iOS 等）

/// 设置Web页面标题（非Web平台为空实现）
void setWebTitle(String title) {
  // 非Web平台，不做任何操作
}

/// 获取当前访问的 URL（非 Web 平台返回 null）
String? getCurrentUrl() {
  // 非 Web 平台不需要获取 URL
  return null;
}

