修复多个问题：启动进度、授权码输入、关闭弹窗位置和进度显示

1. 启动进度提示优化：
   - 保持旋转动画图标（不替换为静态图标）
   - 添加完成图标（对勾），初始化完成后显示
   - 初始化完成后2秒自动隐藏进度提示

2. 移除授权码输入限制：
   - 移除PreviewTextInput事件处理
   - 移除XAML中的PreviewTextInput事件绑定
   - 允许用户任意输入（只自动去除空格）

3. 修复关闭弹窗位置：
   - 设置WindowStartupLocation="Manual"
   - 在Loaded和OnSourceInitialized中设置窗口位置和大小
   - 确保关闭弹窗在应用内（与父窗口完全重叠）
   - 设置Topmost=true确保在最上层

4. 修复关闭程序时显示进度弹窗：
   - 使用Invoke替代BeginInvoke确保立即显示进度遮罩
   - 添加日志记录，便于调试
   - 确保进度遮罩正确显示

5. 修复启动时点击关闭的问题：
   - 在OnClosing中，即使关闭处理器未初始化也显示关闭确认对话框
   - 确保启动时点击关闭也能显示弹窗

